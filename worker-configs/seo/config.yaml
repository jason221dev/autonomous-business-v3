model:
  provider: nvidia_nim
  name: google/gemma-3-27b-it
  max_tokens: 48000
  temperature: 0.5
  base_url: https://integrate.api.nvidia.com/v1
routing:
  fast_model: google/gemma-3-12b-it
  fast_threshold: 8000
toolsets:
  - terminal
  - file
  - web
session:
  persist: true
  resume: true
skills:
  - xurl
  - blogwatcher
  - arxiv
  - maps
heartbeat:
  interval_minutes: 30
  timeout_seconds: 300
